 salt←CreateSalt length;⎕RL;alp
⍝ Creates randomly a salt with `length` characters. Only ASCII chars and digits are used as input.
 ⎕RL←⍬ 2    ⍝ The operating system's random number generator: cannot be seeded, so salts are unpredictable
 alp←(⎕C ⎕A),⎕A,⎕D
 salt←alp[?length⍴≢alp]
