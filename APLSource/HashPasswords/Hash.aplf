 hash←Hash password
⍝ Returns the hash for `password`.\\
⍝ In case the namespace has not yet been initialized, `Hash` takes care of that.
 :If 0=⎕NC'SHA256'
     Init_SHA256 ⍬
 :EndIf
 hash←⎕UCS SHA256.ComputeHash⊂⎕UCS password
