 hash←Hash password
⍝ Returns the SHA-256 hash of `password`, encoded as UTF-8, as 64 lowercase hex digits.\\
⍝ If `Init_SHA256` has not been called yet, `Hash` calls it.
 :If 0=⎕NC'SHA256'
     Init_SHA256 ⍬
 :EndIf
 hash←,Hex SHA256.ComputeHash⊂'UTF-8'⎕UCS password
 ⍝Done
